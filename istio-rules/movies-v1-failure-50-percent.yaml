apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: movies
spec:
  hosts:
    - movies
  http:
    - route:
        - destination:
            host: reviews
            subset: v1
      fault:
        abort:
          percentage:
            value: 50
          httpStatus: 503
